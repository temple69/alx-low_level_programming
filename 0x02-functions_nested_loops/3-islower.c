#include "main.h"
/**
 * _islower - checks if a character is lowercase
 * @c : placeholder for the character to be checked
 * Return: int
 */
int _islower(int c)

{
if (c >= 97 && c <= 122)
{
	return (1);
}
else
{
	return (0);
}


}
